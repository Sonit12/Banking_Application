# Banking_Application
**Description:**  This is a simple Python banking application that allows the user to check their balance, deposit money, withdraw money, and exit the program. It uses a menu-driven interface with a `while` loop and `if-elif-else` statements to perform basic banking operations.
